# Handoff — ย้ายมาทำต่อใน Claude Code

เอกสารนี้สรุปสิ่งที่ทำมาแล้วและสิ่งที่ค้างอยู่ เพื่อให้ทำต่อได้โดยไม่ต้องเล่าใหม่
**เริ่มที่นี่:** ให้ Claude อ่าน `HANDOFF.md` + `SKILL.md` ก่อนทำอะไร

---

## 1. สกิลนี้คืออะไร

ตรวจคุณภาพงานเว็บที่สร้างด้วย AI ก่อนส่งมอบ แบ่งผลเป็น 3 กลุ่มคะแนน
🧠 **UX** (ใช้งานได้และตอบโจทย์) · 🎨 **UI** (หน้าตาและการนำเสนอ) · ⚙️ **Technical** (functional / perf / SEO)

น้ำหนักคะแนนแยกตาม **ประเภทเว็บ** — 🅐 Information / 🅑 E-Commerce / 🅒 Web Application
ส่วน **Track** (🟦 Product ของเราเอง iterate ต่อ / 🟨 Project งานลูกค้า one-off) ไม่ใช่หมวดคะแนนแล้ว แต่กำหนดความเข้มของคำตัดสินและเนื้อหาแท็บ "ความพร้อม & รอบถัดไป"

อ้างอิงมาตรฐานทีม: https://github.com/BearyCenter/uxui
(`sellsuki-product-standard` และ `sellsuki-project-standard`)

---

## 2. ไฟล์ในโฟลเดอร์นี้

```
web-quality-review/
├── SKILL.md                      สกิลหลัก — ขั้น 0–8
├── README.md                     ภาพรวมสำหรับทีม
├── HANDOFF.md                    ไฟล์นี้
├── assets/
│   ├── report-template.html      ต้นแบบรายงาน (คัดลอกโครง+CSS ห้ามออกแบบใหม่)
│   └── ui-probe.js               สคริปต์วัดค่า UI จากหน้าที่เรนเดอร์แล้ว
└── scripts/
    └── run-ui-probe.py           รัน probe 4 breakpoint + screenshot + summary.md
```

---

## 3. ติดตั้งและใช้ใน Claude Code

```bash
# ติดตั้งสกิลระดับโปรเจกต์
cd path/to/your-project
mkdir -p .claude/skills
git clone https://github.com/Krittiya-Amm/web-quality-review.git .claude/skills/web-quality-review

# ติดตั้ง playwright ครั้งเดียว (จำเป็นสำหรับตรวจกลุ่ม UI)
pip install playwright && playwright install chromium
```

เรียกใช้:
```
/web-quality-review
```
หรือพิมพ์ธรรมดา `ตรวจเว็บนี้ก่อนส่งลูกค้า https://...`

**ตรวจ UI ให้ได้คะแนนจริง** — รัน probe ก่อนแล้วให้ Claude อ่านผล:
```bash
python .claude/skills/web-quality-review/scripts/run-ui-probe.py https://example.com --out ./qa-output
```
แล้วบอก Claude ว่า `อ่าน ./qa-output/summary.md และ shot-*.png แล้วให้คะแนนกลุ่ม UI`

---

## 4. ทำไมต้องย้ายมา Claude Code

สกิลนี้พัฒนาในแชท ซึ่งเจอข้อจำกัด 2 อย่างที่ Claude Code ไม่มี:

| ข้อจำกัดในแชท | ใน Claude Code |
|---|---|
| ออกเน็ตไปเว็บภายนอกไม่ได้ (`x-deny-reason: host_not_allowed`) ทำให้ตรวจกลุ่ม UI ไม่ได้เลย | เครื่องคุณออกเน็ตปกติ — ส่งลิงก์ไหนก็ตรวจได้ |
| รัน probe ไม่ได้ → กลุ่ม UI เป็น Not Tested ทั้งกลุ่ม (33–38 คะแนน) | รัน `run-ui-probe.py` ได้ → วัด contrast / touch target / token จริง 4 breakpoint |

**ผลคือ:** ตรวจในแชทได้แค่กลุ่ม UX + Tech · ใน Claude Code ได้ครบทั้ง 100 คะแนน

---

## 5. สถานะปัจจุบันและสิ่งที่ค้าง

### ค้างต้องแก้ในสกิลเอง
1. **`assets/report-template.html` ไม่ผ่านเกณฑ์ที่ตัวมันใช้ตรวจคนอื่น** — รัน probe กับ template (ล่าสุด 2026-09-16) แล้วพบ
   - contrast ไม่ผ่าน WCAG AA **69 จุด** (สาเหตุหลัก: ตัวอักษร 9.5–12.8px สีอ่อน)
   - touch target เล็กกว่า 44px **6–7 จุด** ทุก breakpoint
   - element ล้นขอบที่ 375px **3 จุด**
   - ค่าที่ใช้จริงเยอะเกินระบบ: สีข้อความ 13 · สีพื้น 19 · radius 12 · spacing 17
   → **งานแรกที่ควรทำ:** แก้ template ให้ผ่าน probe ของตัวเอง แล้วรัน `run-ui-probe.py assets/report-template.html` ยืนยัน
   *(หมายเหตุ: "รูปแตก 5" ที่ probe รายงานเป็น false positive — เป็น base64 placeholder ในตัว template เอง)*

2. **เกณฑ์ในหัวข้อ UI1 ของ `summary.md` ยังเป็นค่าประมาณ** (สีเกิน 8 = ไม่ได้มาจาก token) ควรปรับให้อ้าง DS จริงของ Sellsuki เมื่อเข้าถึง DS ได้

3. **ยังไม่มี eval** — ควรทำชุดเว็บทดสอบที่รู้คำตอบ แล้ววัดว่าสกิลจับได้ครบไหม (ใช้ skill-creator ช่วยได้)

### เคสที่ตรวจไปแล้ว
ผลการตรวจของแต่ละงานเก็บไว้ในเครื่อง (`qa-output/` — อยู่ใน `.gitignore`) ไม่ขึ้น repo สาธารณะ
ถ้าต้องการใช้เป็น regression test ให้ดูจากโฟลเดอร์นั้นในเครื่องที่ตรวจ

---

## 6. การตัดสินใจสำคัญที่ผ่านมา (อย่าย้อนโดยไม่มีเหตุผล)

| เรื่อง | ตัดสินใจ | เพราะ |
|---|---|---|
| จำนวนข้อตรวจ | 95 → 29 → ปัจจุบัน 34–35 ข้อต่อรอบ | 95 ข้อไม่มีใครอ่านจบ และหลายข้อซ้ำกัน |
| หมวด AI-Specific | **ยุบทิ้ง** แล้วกระจายข้อ 🤖 ไปหมวดที่เกี่ยว | เป็นเรื่องคร่อมหมวด แยกเป็นหมวดทำให้ซ้ำ |
| กฎ 50% | **ยกเลิก** เปลี่ยนเป็นแสดง coverage รายหมวด | กฎเดิมซ่อนข้อที่ไม่ผ่านจริง (ปัญหา icon ผิดความหมายหายไปจากคะแนน) |
| รูปแบบรายงาน | HTML แบบ PageSpeed + แท็บ (รอบแรก 5 · ตรวจซ้ำ 6) | รายงานเดิมสูง 4,700px เลื่อนอ่านไม่จบ · แท็บลดเหลือ ~1,600px |
| คะแนนตรวจไม่ได้ | ต้องแสดง coverage เสมอ · ห้ามให้คะแนนกลุ่ม UI ถ้ารัน probe ไม่ได้ | เคยรายงาน UI = 0 ทั้งที่ยังไม่ได้ตรวจ ทำให้เข้าใจผิดว่างานดีไซน์แย่ |
| Δ ระหว่างรอบ | ถ้าเปลี่ยนโมเดลคะแนน **ห้ามแสดง Δ เป็นตัวเลข** | เคยมีเคสคะแนนลดลงระหว่างรอบ ทั้งที่งานไม่ได้แย่ลง — เพราะเปลี่ยนวิธีคิดคะแนน ไม่ใช่ถอยหลัง |

---

## 7. Version history

- **v6.0** ตรวจแบบ **link-only** — ถอดชุดเฉพาะ Track ออกจากคะแนน (Track เหลือไว้กำหนดความเข้มของคำตัดสิน) · น้ำหนักใหม่ UX/UI/Tech = 47/38/15 (🅐) · 56/33/11 (🅑) · 61/31/8 (🅒) · เพิ่มแท็บ **🕐 ประวัติ** สำหรับรายงานตรวจซ้ำ และแท็บ **📸 ภาพประกอบ** ที่บังคับแนบหลักฐานเป็น base64 ทุกรอบ
- **v5.2** เพิ่ม `ui-probe.js` + `run-ui-probe.py` — ทำให้คะแนน UI วัดได้จริง
- **v5.1** เพิ่มตารางระดับการเข้าถึง (URL / source / Claude Code / screenshot ได้อะไรบ้าง)
- **v5.0** แบ่งคะแนนเป็น 3 กลุ่ม UX / UI / Technical · ยุบหมวด AI-Specific กระจายเป็น 🤖
- **v4.1** รายงานเป็นแท็บ ลดความสูง 4,700 → 1,600px
- **v4.0** ตัดเหลือแกน 29 ข้อ · เลิกกฎ 50%
- **v3.0** Core 10 → 7 หมวด · 95 → 49 ข้อ
- **v2.2** รายงาน HTML แบบ PageSpeed Insights
- **v2.0** เพิ่มมิติ Product / Project track
- **v1.0** รวมสกิลเดิม 2 ตัว (full + lite) เป็นเวอร์ชันเดียว
