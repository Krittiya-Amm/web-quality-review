---
name: web-quality-review
description: ตรวจคุณภาพงานเว็บ/UI ที่สร้างหรือแก้ด้วย AI ก่อนส่งมอบ (pre-delivery QA review) แบ่งผลเป็น 3 กลุ่มคะแนน — 🧠 UX (ใช้งานได้และตอบโจทย์), 🎨 UI (หน้าตาและการนำเสนอ), ⚙️ Technical & Delivery — และแบ่งตาม Track (Product = ของเราเอง iterate ต่อ / Project = งานลูกค้า one-off) กับประเภทเว็บ (Information, E-Commerce, Web Application) จะถาม track และประเภทเว็บก่อนเริ่มตรวจเสมอ แล้วเช็คว่ามี IA / User Flow / Design Spec ไหม ให้ใช้ทุกครั้งที่ผู้ใช้ขอ "ตรวจงานก่อนส่งลูกค้า", "ตรวจก่อน merge / ship", "รีวิวคุณภาพเว็บ", "QA เว็บ", "เช็คความพร้อมก่อน deliver / handoff", "quality review", "ตรวจ UX UI", "ตรวจ DS compliance", "ตรวจ CI", "ตรวจ responsive / accessibility / SEO", "internal test ก่อน UAT" หรืออัปโหลด URL/โค้ด/Jira card แล้วถามว่าพร้อมส่งหรือยัง ผลลัพธ์เป็นไฟล์ HTML แบบ PageSpeed Insights พร้อมคะแนน UX / UI / Technical แยกกัน และคะแนนรวม 100
---

# Web Quality Review

ตรวจงานเว็บที่สร้างด้วย AI ก่อนปล่อย — หาปัญหาที่กระทบผู้ใช้และธุรกิจ, เทียบกับ Requirement และ Design System, จัดลำดับความสำคัญ, บอกวิธีแก้ที่ลงมือได้ทันที

**ตำแหน่งในกระบวนการ** — 🟦 Product: หลัง Vibe Code ก่อน merge/ship · 🟨 Project: Stage 5.1 Internal Test ก่อนส่งลูกค้า UAT (round budget S=1, M=2, L=3)
อ้างอิง `https://github.com/BearyCenter/uxui`

**หลักคิด:** งาน AI มีจุดอ่อนเฉพาะตัว — ข้อมูลที่แต่งขึ้นเอง, เศษโค้ด, interaction ที่ดูเหมือนทำงานแต่กดแล้วไม่มีผล, hardcode ค่าที่ควรมาจาก token จึงต้อง **ตรวจของจริง** ทุกข้อที่ไม่ผ่านต้องชี้ตำแหน่งและบอกวิธีแก้
ข้อที่เป็นความเสี่ยงเฉพาะงาน AI ทำเครื่องหมาย 🤖 ไว้ — กระจายอยู่ในหมวดที่เกี่ยวข้อง ไม่แยกเป็นหมวดของตัวเอง

---

## ขั้น 0 — ถามก่อนเสมอ (ห้ามข้าม)

ถามด้วย `ask_user_input_v0` **แล้วหยุดรอคำตอบ** แค่ 2 ข้อ:

**1. Track** — 🟦 Product (core platform, internal tools · key `SUKI` `OC` `MNG`) หรือ 🟨 Project (งานลูกค้า one-off · key `CLIENT-XXX`) · ไม่แน่ใจ → default Product แต่ต้อง confirm
   *Track ไม่ใช่หมวดคะแนนแล้ว (ตรวจผ่าน URL อย่างเดียว) — ใช้กำหนดความเข้มของคำตัดสินและเนื้อหาแท็บ "ความพร้อม & รอบถัดไป" (Product → Next Iteration · Project → Handoff Readiness) เท่านั้น*

**2. ประเภทเว็บ** — 🅐 Information (มาอ่าน) · 🅑 E-Commerce (ตะกร้า/ชำระเงิน) · 🅒 Web Application (ล็อกอิน/CRUD/หลายขั้น)
ไม่แน่ใจ → มีตะกร้า = 🅑 · มีล็อกอิน = 🅒 · อ่านเป็นหลัก = 🅐 · ผสมกันเลือกตัวเสี่ยงสูงสุด (🅑 > 🅒 > 🅐)

**ขนาดงานและขั้นของงาน ประเมินเองแล้วแจ้ง ไม่ต้องถาม** — Product: S/M/L จาก story points/AC/หน้าจอที่กระทบ · Project: S=1 หน้า, M=2–5, L=6–15, Enterprise=16+

---

## ขั้น 1 — Readiness Pre-check

เช็คว่ามี artifact ต้นน้ำไหม ถ้าไม่มีจะตรวจได้แค่ "งานดูดีไหม" ไม่ใช่ "ตรงกับที่ออกแบบไว้ไหม"

| Artifact | 🟦 Product | 🟨 Project |
|---|---|---|
| Brief + Acceptance Criteria | ต้องมี | ต้องมี |
| **IA / Sitemap** | **ต้องมี** | **ต้องมี** |
| **User Flow** | ควรมี (L ต้องมี) | **ต้องมี** (🅑 purchase · 🅒 ทุก role) |
| Design Spec | + ระบุ **DS 1.0 / 2.0** | + **variant ที่ลูกค้าเลือก** |
| Usability / State | Persona Test (M/L ต้องมี) + must-fix ปิดครบ | State / Edge case list |
| ก่อนส่ง | — | Staging URL + Deploy checklist + Test Scenario + Handoff Section F |

สรุปเป็น ✅ มี / ❌ ไม่มี / ➖ ไม่จำเป็น แล้วระบุว่าขาดอะไรทำให้ตรวจอะไรไม่ได้
Product ที่ขาด Persona Test ใน M/L = **ผิดกฎ A1/A5** ถือเป็นปัญหากระบวนการระดับ High
**ห้ามปฏิเสธการตรวจเพราะ artifact ไม่ครบ**

---

## ขั้น 2 — Input

ต้องมี **URL / โค้ด / repo** · มีให้ยิ่งดี: Jira card หรือ AC, Design System หรือ CI ลูกค้า, device/browser ที่ต้องรองรับ
ไม่ครบก็ตรวจได้ แต่รายงานต้องแยก **"ข้อมูลที่ได้รับ"** ออกจาก **"สมมติฐานที่ใช้ตรวจ"**

### ระดับการเข้าถึง — ตัวกำหนดว่าจะตรวจ UI ได้ไหม
**แจ้งผู้ใช้ตั้งแต่ต้นว่ากลุ่ม 🎨 UI และ UX3 ตรวจได้แค่ไหน ขึ้นกับสิ่งที่ได้รับ** ห้ามปล่อยให้เข้าใจว่าคะแนน UI ต่ำเพราะงานแย่

| ได้รับอะไร | ตรวจ UX ได้ | ตรวจ UI ได้ | หมายเหตุ |
|---|---|---|---|
| **URL อย่างเดียว** | เนื้อหา + IA เท่านั้น | **แทบไม่ได้** | ดึงได้แค่ข้อความที่สกัดแล้ว ไม่มี CSS ไม่มีหน้าที่เรนเดอร์ · **ต้องบอกผู้ใช้ว่าคะแนน UI รอบนั้นไม่มีความหมาย** |
| **ไฟล์ source / zip โปรเจกต์** | ได้เกือบครบ | **ได้เกือบครบ** | เสิร์ฟบน localhost แล้วรัน playwright ได้ ไม่ต้องพึ่งเน็ตภายนอก |
| **รันใน Claude Code ที่ repo** | ครบ | ครบ | ดีที่สุด — มี git history ตรวจ regression ได้ด้วย |
| **Screenshot 4 breakpoint** | — | ได้ที่ตาเห็น | วัด contrast/token เป็นตัวเลขไม่ได้ |

**ถ้าได้แค่ URL แล้วผู้ใช้ต้องการคะแนน UI ให้ขอสิ่งใดสิ่งหนึ่งข้างบนก่อน** อย่าตรวจไปแล้วรายงาน UI = 0

ถ้ารันโค้ดได้ ใช้เต็มที่:
- grep `TODO` `console.log` `href="#"` `lorem` API key · หา handler ว่างและ mock data
- **ดึง computed style ทุก element** มาเทียบ token ทีละค่า (hex, font-family, font-size, line-height, spacing)
- **screenshot ที่ 375 / 768 / 1440 / 1920** ตรวจ horizontal scroll, ข้อความล้น, layout จัดใหม่จริงไหม
- **คำนวณ contrast ratio** ทุกคู่สีเทียบ WCAG AA เป็นตัวเลข · วัด touch target ว่าถึง 44px
- หารูปแตกด้วย `naturalWidth === 0` · อ่าน console error
- **กด interaction จริง** (modal, filter, multi-step, ปุ่มสลับภาษา) เพื่อปลดล็อก UX3
- ระวัง: ถ้าอยู่ในสภาพแวดล้อมที่ network มี allowlist โดเมน การเข้าเว็บ live จะได้ `403 host_not_allowed` — ทดสอบก่อนด้วย `curl -sSI <url>` แล้วดู header `x-deny-reason` · ถ้าโดนบล็อก ให้ขอไฟล์ source มาเสิร์ฟ localhost แทน หรือแจ้งผู้ใช้ให้ติดต่อ organization owner เพิ่มโดเมนเข้า allowlist

### ใช้ `assets/ui-probe.js` เพื่อวัดค่า UI จริง (ทำให้คะแนน UI มีความหมาย)
สคริปต์นี้เก็บค่าที่ตาคนวัดเองไม่ได้ ใช้ได้ทั้ง 3 ทางโดยผลลัพธ์เหมือนกัน:
- **Claude Code / playwright** — `await page.evaluate(uiProbe)`
- **Claude in Chrome** — วางลง `javascript_tool`
- **ให้ผู้ใช้รันเอง** — วางใน DevTools Console แล้วส่ง JSON กลับมา

**ต้องรันซ้ำที่ 375 / 768 / 1440 / 1920 px** แล้วเทียบผลทุก breakpoint — ค่าที่เปลี่ยนตาม viewport คือหลักฐานว่า layout จัดใหม่จริงหรือแค่ย่อ desktop

สคริปต์คืนค่าที่แมปตรงกับ checklist:
| ผลลัพธ์ | ใช้ตัดสินข้อไหน |
|---|---|
| `tokens.uniqueCounts` (จำนวนสี/ฟอนต์/radius/spacing ที่ใช้จริง) | UI1 — ถ้าสีข้อความเกิน ~8 ค่า หรือ radius เกิน ~4 ค่า แปลว่าไม่ได้มาจาก token |
| `tokens.textColors` / `bgColors` (hex + จำนวนครั้ง) | UI1 — เทียบกับ palette ของ DS หรือ CI ทีละค่า หาสี "ใกล้เคียงแต่ไม่ใช่" |
| `horizontalScroll`, `overflowing` | UI2 — มี element ล้นขอบจอไหม พร้อม selector |
| `smallTargets` | UI2 — ปุ่ม/ลิงก์ที่เล็กกว่า 44px พร้อมขนาดจริง |
| `typography.fontSizes` / `bodyBelow16` / `lineHeights` | UI3 — body ต่ำกว่า 16px จุดไหน |
| `contrastFails` (ratio + คู่สี + ข้อความ) | UI3 — คำนวณเทียบ WCAG AA ให้แล้ว รวม large-text exception |
| `media.broken` / `missingAlt` / `formats` / `noDimensions` / `notLazy` | UI3 + T1 — รูปแตกจริง, ขาด alt, ยังเป็น PNG/JPG |
| `headings` | UI3 — H1 เกิน 1 หรือลำดับกระโดด |
| `seo` | T1 — og:image, canonical, jsonLd, favicon, lang ยืนยันได้แน่นอน |

**ถ้ารัน probe ไม่ได้ ห้ามให้คะแนนกลุ่ม UI** ให้ทั้งกลุ่มเป็น ⚪ Not Tested แล้วบอกผู้ใช้ว่าต้องมีอะไรจึงจะตรวจได้

---

## ขั้น 3 — 🧠 UX Checklist (ใช้งานได้และตอบโจทย์)

แต่ละข้อ: ✅ ผ่าน / ❌ ไม่ผ่าน / ⏭️ ตรวจไม่ได้ (ระบุเหตุผล) — **ห้ามให้ผ่านกับข้อที่ไม่ได้ตรวจจริง**

### UX1 · Requirement & Content
- [ ] มีหน้า/section/ฟีเจอร์ ครบตาม Requirement หรือ AC ทุกข้อ · CTA หลักชัดเจนและเจาะจง (เลี่ยง "Learn More" ลอยๆ)
- [ ] 🤖 **ไม่มีข้อมูลที่ AI แต่งขึ้นเอง** — บริการ ราคา รีวิว สถิติ พาร์ตเนอร์ · **ตัวเลขทุกตัวมีที่มาและตรงกับสิ่งที่หน้าเว็บแสดงจริง**
- [ ] ไม่มี placeholder ตกค้าง · ไม่มีคำผิด (เฉพาะชื่อแบรนด์) · ข้อมูลติดต่อ เวลาทำการ และวันที่ถูกต้อง
- [ ] Tone สม่ำเสมอ ไม่เกินจริง · **เนื้อหาไม่ขัดแย้งกันเองระหว่างจุดต่างๆ ในเว็บ**

### UX2 · IA & Navigation
- [ ] Navigation เข้าใจง่าย · **ชื่อเมนูตรงกับเนื้อหาในหน้า**
- [ ] โครงเรียงตามลำดับความสำคัญ ไม่ซ้ำ · ตรงกับ IA ที่วางไว้
- [ ] 🤖 ทุกลิงก์ในเมนูและ CTA พาไปยังปลายทางที่มีอยู่จริงและถูกต้อง
- [ ] Footer และจุดทางออกมีข้อมูลที่จำเป็น (รวมหน้านโยบายที่ต้องมี)

### UX3 · Flow, States & Interaction
- [ ] **Flow หลักทำได้จนจบ** ไม่มี dead end · ย้อนกลับได้โดยข้อมูลไม่หาย
- [ ] **ทุกหน้าจอมี 6 states ครบ** (กฎ Q1): Default · Loading · Empty · Error · Success · Disabled
- [ ] Edge case ครอบคลุม (กฎ Q6): ข้อความยาวเกิน · ข้อมูล = 0 · ไม่มีสิทธิ์ · network error
- [ ] Feedback หลังทำรายการแจ้งชัดทุกครั้ง ไม่เงียบ · destructive action มีหน้าต่างยืนยัน
- [ ] Keyboard navigate ได้ · modal ปิดด้วย keyboard ได้ · ลำดับ focus สมเหตุสมผล
- [ ] 🤖 **ไม่มี interaction ที่ดูเหมือนทำงานแต่กดแล้วไม่มีผล** · ไม่มี element โชว์ demo ที่ยังไม่เชื่อมข้อมูลจริง · ไม่มี mock data หลุด

### UX4 · ชุดเฉพาะประเภทเว็บ

**🅐 Information**
- [ ] ตัวเลข/สถิติมีแหล่งอ้างอิง · รีวิวเป็นของจริงและได้รับความยินยอม · โลโก้พาร์ตเนอร์ได้รับอนุญาต
- [ ] มีนโยบายความเป็นส่วนตัว + เงื่อนไขการใช้งาน + consent ถ้าเก็บข้อมูล · ฟอร์มติดต่อส่งถึงจริงและมีข้อความยืนยัน
- [ ] เนื้อหายาวมีสารบัญและวันที่อัปเดต · บทความมีทางไปต่อที่ชัดเจน

**🅑 E-Commerce**
- [ ] **ราคาสอดคล้องทุกจุด** (ลิสต์ = หน้าสินค้า = ตะกร้า = สรุปคำสั่งซื้อ) · ส่วนลดคำนวณถูก · **ค่าส่ง/ภาษี/ค่าธรรมเนียมแสดงก่อนกดจ่าย ไม่มีค่าซ่อน**
- [ ] **ซื้อได้จนจบ**: ตะกร้าเพิ่ม/ลด/ลบได้และคงอยู่เมื่อ refresh → checkout ย้อนแก้ได้ → success/fail ชัด + เลขคำสั่งซื้อ + ส่งยืนยัน · ไม่ตัดเงินซ้ำเมื่อกดรัว
- [ ] หน้าสินค้าครบ (ชื่อ ราคา รูปหลายมุม รายละเอียด ตัวเลือก) · สถานะสินค้าถูก · **กดซื้อของที่ยังไม่ขายไม่ได้**
- [ ] นโยบายครบ: จัดส่ง · **คืน/เปลี่ยนพร้อมจำนวนวันชัดเจน** · รับประกัน · PDPA แจ้ง+consent · **ไม่มี urgency ปลอม** (countdown ที่ไม่นับจริง = dark pattern)

**🅒 Web Application**
- [ ] Auth & Role: สมัคร/เข้า/ออก/ลืมรหัสผ่านครบ · session หมดอายุมีแจ้งไม่เด้งเงียบ · **เข้า URL ตรงๆ โดยไม่มีสิทธิ์ต้องถูกบล็อก ไม่ใช่แค่ซ่อนปุ่ม**
- [ ] Multi-step & Form: เห็นว่าอยู่ขั้นไหน · draft/autosave · validate ทันทีและบอกวิธีแก้ · กันกดส่งซ้ำ · ข้อมูลไม่หายเมื่อ validate ไม่ผ่าน
- [ ] CRUD & Table: บันทึกจริงและเห็นผลทันที · ตารางเรียง/กรอง/ค้นหา/แบ่งหน้า/bulk ทำงานจริง · จำ filter เมื่อกลับมา
- [ ] Data integrity: ข้อมูลที่แสดงตรงกับที่บันทึก · วันที่/ตัวเลข/สกุลเงิน/timezone ถูกและสม่ำเสมอ

---

## ขั้น 4 — 🎨 UI Checklist (หน้าตาและการนำเสนอ)

### UI1 · Design System & Visual Consistency
- [ ] **สีและฟอนต์มาจาก token หรือ CI ทุกค่า** — ไม่ hardcode, ไม่มีสี "ใกล้เคียงแต่ไม่ใช่"
- [ ] Component เดียวกันหน้าตาเหมือนกันทุกที่ · ไม่สร้าง component ใหม่ที่ซ้ำของเดิม
- [ ] Visual hierarchy, spacing, alignment สม่ำเสมอ · grid มาตรฐานเดียว · radius/shadow ไม่หลากหลายเกิน
- [ ] Icon สไตล์และขนาดเดียวกัน · สไตล์ภาพประกอบไปทางเดียวกัน
- [ ] 🤖 **ไม่ดูเหมือน Template AI** — card ซ้ำทุก section, center ทุกอย่าง, icon ทุกหัวข้อ, hero ใหญ่เกิน, gradient เยอะเกิน

### UI2 · Responsive Layout
ตรวจ 375 / 768 / 1440 / 1920 px
- [ ] ไม่มี horizontal scroll · ข้อความไม่ล้น/ถูกตัด · รูปไม่ผิดสัดส่วน
- [ ] 🤖 **Layout จัดใหม่จริงตาม breakpoint ไม่ใช่แค่ย่อ desktop ลงมา** · ตาราง/modal/sticky ใช้บนมือถือได้
- [ ] Touch target ≥ 44px · action สำคัญยังมองเห็น · padding ด้านข้างเหมาะสม

### UI3 · Typography, Contrast & Media
- [ ] Body ≥ 16px · line-height ≥ 1.5 · ความยาวบรรทัดพออ่าน (45–90 ตัวอักษร) · heading ไม่กระโดด
- [ ] Contrast ผ่าน WCAG AA · **error และสถานะไม่สื่อด้วยสีอย่างเดียว**
- [ ] รูปทุกรูปโหลดได้และไม่ผิดสัดส่วน · **รูปมี alt** · heading เรียง H1–H6 (H1 เดียวต่อหน้า)
- [ ] 🤖 **Icon และรูปไม่ผิดความหมาย** (เช่น ไอคอน PDPA ใช้กับสินค้าที่ไม่เกี่ยวกัน) · รูปตรงกลุ่มเป้าหมาย

---

## ขั้น 5 — ⚙️ Technical Checklist

### T1 · Functional, Performance & SEO
- [ ] ลิงก์และปุ่มทำงานจริงทุกจุด · ไม่มี `href="#"` · logo กลับหน้าแรก · **ลิงก์ภายนอกชี้ปลายทางจริง ไม่ใช่หน้าค้นหา**
- [ ] Modal/accordion/tab/carousel/filter ทำงานถูก · ไม่มี console error · refresh ไม่หน้าขาว · มีหน้า 404
- [ ] รูป optimize (WebP/AVIF) + width/height + lazy load · layout ไม่ขยับตอนโหลด · ไม่มี library ใหญ่ที่ไม่ได้ใช้
- [ ] มี title + meta description + canonical + **og:image** + favicon · มี structured data ถ้าเป็นเว็บ public
- [ ] 🤖 ไม่มี `console.log` / `TODO` / API key hardcode — **เจอ API key = Blocker ทันที**

> **หมายเหตุ — สกิลนี้ตรวจผ่าน URL อย่างเดียว (link-only)** จึงไม่มีหมวดที่ต้องใช้ repo/source ในการให้คะแนน · เรื่องที่ต้องเปิด repo ถึงตรวจได้ (DS version compliance, regression กับหน้าอื่น, CI/CD, maintainability, deliverable Section F, สิทธิ์ asset) **อยู่นอกขอบเขตการให้คะแนน** — ถ้าจำเป็นให้ตรวจแยกด้วยมือ/ในรอบ code review และบันทึกไว้ในแท็บ "ความพร้อม & รอบถัดไป" เป็นข้อสังเกต ไม่ใช่คะแนน

---

## ขั้น 6 — ระดับความรุนแรง

- 🛑 **Blocker** — ปล่อยไม่ได้: งาน error · mobile ใช้ไม่ได้ · ข้อมูลสำคัญผิด · API key หลุด
  🅐 ข้อมูล/สถิติปลอม · 🅑 ซื้อไม่จบ, ราคาไม่ตรง, จ่ายแล้วไม่ยืนยัน, ตัดเงินซ้ำ · 🅒 ล็อกอินไม่ได้, ข้อมูลหายกลางทาง, เห็นข้อมูลข้าม role
  *(regression/DS version/CI/deliverable/asset rights ต้องเปิด repo ถึงตรวจได้ — อยู่นอกขอบเขต link-only ให้ตรวจแยกด้วยมือ)*
- 🔴 **High** — กระทบผู้ใช้/conversion มาก: action หลักไม่ชัด, nav สับสน, contrast อ่านไม่ได้, responsive แตกหน้าหลัก, ขาด error state, ข้อมูลขัดแย้งกันเอง, ไม่มี og:image · สี/โลโก้/แบรนด์ผิดจากที่เห็นชัด (จับใน UI1)
- 🟡 **Medium** — กระทบคุณภาพและความสม่ำเสมอ: spacing ไม่ตรง, state ไม่ครบบางจุด, copy ไม่ชัด, ข้อมูลไม่สม่ำเสมอระหว่างรายการ
- ⚪ **Low** — เก็บงานให้เนี้ยบ: icon ไม่ตรงเล็กน้อย, alignment คลาดเคลื่อน

---

## ขั้น 7 — คะแนน: UX / UI / Technical แยกกัน รวม 100

| กลุ่ม | หมวด | 🅐 Info | 🅑 E-Com | 🅒 Web App |
|---|---|---|---|---|
| 🧠 **UX** | UX1 Requirement & Content | 19 | 14 | 12 |
| | UX2 IA & Navigation | 14 | 12 | 12 |
| | UX3 Flow, States & Interaction | 7 | 12 | 16 |
| | UX4 ชุดเฉพาะประเภทเว็บ | 7 | 18 | 21 |
| | **รวม UX** | **47** | **56** | **61** |
| 🎨 **UI** | UI1 Design System & Visual | 12 | 11 | 12 |
| | UI2 Responsive Layout | 9 | 12 | 9 |
| | UI3 Typography, Contrast & Media | 17 | 10 | 10 |
| | **รวม UI** | **38** | **33** | **31** |
| ⚙️ **Tech** | T1 Functional, Perf & SEO | 15 | 11 | 8 |
| | **รวม Technical** | **15** | **11** | **8** |
| | **รวมทั้งหมด** | **100** | **100** | **100** |

*น้ำหนักสะท้อนธรรมชาติของงาน — Info ให้ UI/readability สูงเพราะคนมาอ่าน · Web App ให้ UX flow สูงเพราะคนมาทำงานให้จบ · Technical มีเฉพาะ T1 (functional/perf/SEO) ที่ตรวจผ่าน URL ได้จริง จึงมีน้ำหนักไม่มาก*

คะแนนหมวด = (ข้อผ่าน ÷ ข้อที่ตรวจได้จริง) × คะแนนเต็ม · ข้อ ⏭️ ไม่นับทั้งตัวตั้งและตัวหาร
**คะแนนกลุ่ม** = (คะแนนที่ได้ในกลุ่ม ÷ คะแนนเต็มของหมวดที่ตรวจได้ในกลุ่ม) × 100
**คะแนนรวม** = คิดแบบเดียวกันจากทุกหมวดที่ตรวจได้

**บังคับแสดง coverage — แสดงที่ระดับกลุ่มเท่านั้น ไม่ต้องมีแถบ coverage แยก (`.cov`) อีก:**
- **รายหมวด** — แถว list แสดงแค่ **ชื่อหมวด + แถบ progress + คะแนน** เท่านั้น · **ห้ามใส่คำอธิบาย/coverage/โน้ตต่อท้ายคะแนน (`.cf`)** — เก็บรายละเอียดว่าตรวจอะไร/เหลืออะไรไว้ในแท็บ "ปัญหาที่ต้องแก้" และ "ผ่านแล้ว"
- **รายกลุ่ม** — กล่องวงกลม 3 กลุ่มแสดง "XX / YY คะแนนที่ตรวจได้" ใต้ชื่อกลุ่ม + ป้าย tot ข้างชื่อกลุ่ม (เช่น `27 / 32`) · **ห้ามแสดงคะแนนลอยๆ**
- ถ้าตรวจจาก URL อย่างเดียว กลุ่ม UI มักตรวจได้น้อยมาก **ต้องบอกตรงๆ ว่าคะแนน UI รอบนั้นเชื่อถือได้ต่ำ** ไม่ใช่ปล่อยให้เข้าใจว่า UI แย่

**คำตัดสิน** (Blocker ตัดสิทธิ์ก่อนคะแนนเสมอ) — ใช้คะแนนรวม:
| คะแนนรวม | 🟦 Product | 🟨 Project |
|---|---|---|
| มี Blocker ≥ 1 | 🚫 ห้าม merge/ship | 🚫 ห้ามส่งลูกค้า |
| 90–100 | ✅ พร้อม merge / ship | ✅ พร้อมส่งลูกค้า UAT |
| 80–89 | ⚠️ ควรแก้ Medium ก่อน | ⚠️ ควรแก้ Medium ก่อน |
| 70–79 | 🔧 ต้องแก้ก่อน merge | 🔧 ต้องแก้ก่อนรอบสุดท้าย |
| < 70 | 🚫 ยังไม่ควร ship | 🚫 ยังไม่ควรส่งมอบ |

ถ้ากลุ่มใดกลุ่มหนึ่งต่ำกว่า 50 ให้ระบุในคำตัดสินว่าปัญหาหลักอยู่ฝั่ง UX หรือ UI เพื่อให้ทีมรู้ว่าใครต้องแก้

---

## ขั้น 8 — รายงาน

**ค่าเริ่มต้น: ไฟล์ HTML** — คัดลอกโครงและ CSS จาก **`assets/report-template.html`** ทั้งหมด เปลี่ยนแค่ข้อมูล **ห้ามออกแบบใหม่เอง**
ถ้าสร้างไฟล์ไม่ได้ → รายงานข้อความ ลำดับเดียวกัน แทนวงกลมด้วยแถบ 10 บล็อก

**ต้องมีรูปประกอบเป็นหลักฐานทุกครั้งที่ตรวจ** — ฝังภาพ screenshot ที่ probe จับไว้ลงในรายงาน โดยฝังเป็น base64 data-URI (`<img src="data:image/jpeg;base64,…">`) ให้ไฟล์รายงานเปิดได้เดี่ยวๆ ไม่ต้องแนบไฟล์ภายนอก · **รวมรูปทั้งหมดไว้ในแท็บแยก "📸 ภาพประกอบ" (แท็บสุดท้ายเสมอ)** ส่วน `.shots` — รอบแรกใช้ pane `.p5` (ไม่มีแท็บประวัติ) · ตรวจซ้ำใช้ `.p6` ต่อจาก `.p5` 🕐 ประวัติ · template wire ทั้งสองกรณีไว้แล้ว ตาม `assets/report-template.html` — ไม่ต้องแทรกรูปในแต่ละ finding แต่ให้ finding อ้างอิงว่า "ดูภาพประกอบในแท็บ 📸"
- **แกลเลอรี breakpoint** (`.bpr`): thumbnail ส่วนบนของหน้า 4 ขนาด 375/768/1440/1920px ครอปเฉพาะ hero เพื่อเทียบ layout responsive
- **ครอปจุดปัญหา** (`.shotfig`): ภาพซูมของ finding ที่พิสูจน์ได้ด้วยภาพ (เช่น contrast, touch target, countdown/โปรบาร์) พร้อม `<figcaption>` บรรยายปัญหา + ตัวเลขที่วัดได้
- ถ้าเป็น **false positive** (เช่น probe อ่านพื้นเข้มเป็นขาว) ให้แนบภาพยืนยันพร้อมหมายเหตุว่าไม่นับคะแนน
- ครอปด้วย PIL/Playwright ให้ขนาดพอเหมาะ (กว้าง ≤ ~720px, JPEG quality ~72) เพื่อคุมขนาดไฟล์ · render-verify ว่า `document.images` โหลดครบ ไม่มี broken และรูปอยู่ในแท็บที่ถูกต้อง

โครงรายงาน — **สรุปด้านบน + แท็บ** (รอบแรก 5 แท็บ · ตรวจซ้ำ 6 แท็บ เพิ่ม 🕐 ประวัติ)

**ส่วนสรุปด้านบน**
- Header 1 บรรทัด + chip: Track (จุดสี ไม่ใช้ emoji สี่เหลี่ยม) / Type / Size / Method / **โมเดล (รีวิวผ่านลิงก์ /100)** / วันที่ · chip ทุกอันสูงเท่ากัน อยู่บรรทัดเดียว label ตัวเล็ก + value ตัวเข้ม
- คะแนนรวม + verdict บรรทัดเดียว + จำนวน issue แต่ละระดับ — **ไม่มีแถบ coverage แยก (`.cov`) และไม่มีย่อหน้าอธิบาย verdict (`.vn`)** ให้เก็บคำอธิบายโมเดล/ข้อยกเว้นไว้ในแท็บ "ความพร้อม & รอบถัดไป" แทน
- **บรรทัด verdict (`.vd`) ให้สั้นเสมอ: `<emoji> <คะแนน> / 100 — <ชื่อ band>` เท่านั้น** เช่น `✅ 94 / 100 — ผ่านเกณฑ์ดีเยี่ยม (≥90)` หรือ `🚫 36 / 100 — ยังไม่ควรส่งมอบ` · **ห้ามต่อท้ายด้วย breakdown** เช่น "· ไม่มี Blocker/High · UX/Tech เต็ม · เหลือเก็บงาน UI" — รายละเอียดพวกนี้อยู่ในจำนวน issue (`.cnt`) และแท็บ "ความพร้อม & รอบถัดไป" อยู่แล้ว
- **วงกลม 3 กลุ่มเรียงข้างคะแนนรวม: 🧠 UX · 🎨 UI · ⚙️ Technical** พร้อมคะแนนเต็มของกลุ่มและ coverage ("XX / YY คะแนนที่ตรวจได้")
- คะแนนรายหมวด (UX1–4, UI1–3, T1) **แสดงเป็น list แยกแถวตามกลุ่ม** (ไม่ใช่วงกลม เพราะวงกลม 3 กลุ่มอยู่ด้านบนแล้ว) — แต่ละแถวมีชื่อหมวด + แถบ progress + คะแนน + coverage · แสดงทุกหมวดแม้ Not Tested (แถบ dashed, คะแนน "—")
- Legend รูปทรง
- **ไม่ใส่บล็อก delta/จุดแข็ง/waived เป็นการ์ดด้านบน** — คะแนนรอบก่อน + Δ ของการตรวจซ้ำ (โมเดลเดียวกัน) ให้อยู่ในแท็บ **🕐 ประวัติ** ไม่ใช่การ์ดสรุปด้านบน · ค่าเริ่มต้นส่วนสรุปไม่มีบล็อกพวกนี้

**แท็บ** (CSS ล้วน ใช้ `input[type=radio]` + label ไม่ใช้ JavaScript)
1. **ปัญหาที่ต้องแก้** — ลิสต์ **ทุกอย่างที่ยังขาดเพื่อให้ได้เต็ม 100** คือทั้ง (ก) บั๊ก/ข้อผิดพลาดจริง และ (ข) งานความสมบูรณ์ (last-mile ที่ยังไม่เก็บคะแนนของแต่ละหมวด) · จัดกลุ่มตามความรุนแรง Blocker / High / Medium / Low **และติดป้าย 🧠 UX / 🎨 UI / ⚙️ Tech ที่ทุกแถว** เพื่อให้ designer กับ dev แยกงานกันได้
   - แถวบั๊กใช้รูปทรงตามระดับ (▲ poor / ■ avg) · **แถวงานความสมบูรณ์ใช้วงกลมประ (`sh da`)** เพื่อแยกให้ชัดว่าเป็น "คะแนนที่ยังไม่เก็บ" ไม่ใช่ของเสีย
   - ทุกแถวมีป้ายคืนคะแนน (+N) และเวลาแก้ · ปิดทุกแถวเป็นค่าเริ่มต้น
   - ข้อที่ทีมยืนยันว่าเป็นข้อมูล/สเปกที่ตั้งใจ (เช่น ตัวเลขที่ทีมเตรียมมา) = **คืนคะแนนให้ผ่าน** ย้ายไปแท็บ "ผ่านแล้ว" พร้อมหมายเหตุ ไม่นับเป็นปัญหา
   - ปิดท้ายด้วยแถบเส้นทางคะแนนแบบไล่ระดับ: **"XX → ~YY (แก้บั๊ก) → 100 (เก็บครบ)"**
2. **ผ่านแล้ว** — บรรทัดเดียวต่อข้อ จัดกลุ่มตาม UX / UI / Tech
3. **ตรวจไม่ได้** — ตาราง รายการ / เหตุผล / วิธีตรวจ + คำเตือนว่าผ่านรีวิวนี้ไม่เท่ากับไม่ต้องทดสอบมือ
4. **ความพร้อม & รอบถัดไป** — Readiness Pre-check + ได้รับ/สมมติฐาน + Next Iteration (Product) หรือ Handoff Readiness (Project)
5. **🕐 ประวัติ** (เฉพาะรายงาน**ตรวจซ้ำ** · รอบแรกไม่มีแท็บนี้) — ไทม์ไลน์ทุกรอบที่ตรวจ (`.hist` / `.hitem`) · แต่ละรอบแสดงคะแนนรวม + คะแนน 3 กลุ่มพร้อม Δ และสรุปสิ่งที่เปลี่ยน (✅ dev แก้จริง · ↩︎ คืนคะแนนให้ผ่าน (ลูกค้า/ทีมยืนยัน) · ⏳ ยังค้าง) · รอบล่าสุดอยู่บนสุด (จุดสีเขียว `.now`)
6. **📸 ภาพประกอบ** — แท็บแยกต่างหาก รวมภาพหลักฐานทั้งหมด: แกลเลอรี breakpoint (`.bpr`) + ครอปจุดปัญหา (`.shotfig`) พร้อม caption (ดูรายละเอียดในหัวข้อรูปประกอบด้านบน)

หน้าควรสูงไม่เกิน ~1,800px ในแท็บแรก · `@media print` ให้กางทุกแท็บออก

**ตรวจซ้ำ** ต้องแสดงคะแนนรอบก่อนและค่า Δ **ของทั้ง 3 กลุ่มและคะแนนรวม ในแท็บ 🕐 ประวัติ** (ไม่ใช่การ์ด delta ด้านบนแล้ว) · ถ้าเปลี่ยนโมเดลคะแนนระหว่างรอบ **ห้ามแสดง Δ เป็นตัวเลข** ให้บอกตรงๆ ว่าเทียบกันไม่ได้และเทียบได้จากรอบถัดไป
**คืนคะแนน** = (คะแนนเต็มหมวด ÷ ข้อที่ตรวจได้) × ข้อที่ issue ทำให้ไม่ผ่าน · ได้ 0 ใส่ `<1` · issue เดียวกระทบหลายหมวดให้รวมทุกหมวด

---

## หลักการ
- **จับต้องได้**: "ปุ่ม 'บันทึก' ใน OrderDetail.tsx บรรทัด 214 มี onClick ว่าง" ดีกว่า "บางปุ่มยังไม่สมบูรณ์"
- **ซื่อสัตย์เรื่องข้อจำกัด**: ตรวจจาก URL จับ UI ได้น้อยมาก ระบุใน Not Tested ทุกครั้ง — ผ่านรีวิวนี้ไม่เท่ากับไม่ต้องทดสอบมือ
- **ตรวจซ้ำต้องบอกตรงๆ ว่าอะไรยังไม่ได้แก้** — ถ้ารอบก่อนแจ้ง High ไว้แล้วรอบนี้ยังอยู่ ให้ระบุชัดว่าเป็นข้อค้างจากรอบก่อน ไม่กลืนไปกับ issue ใหม่
- **ไม่ดราม่าเกินจริง**: Low คือ Low
- **เชื่อมกลับ flow**: ปัญหาที่ควรถูกจับตั้งแต่ต้นน้ำ (IA ผิด, flow ตัน, ข้าม persona test) ให้ระบุว่าเป็นปัญหากระบวนการ
- **บอกว่าใครต้องแก้**: ใช้ป้าย UX / UI / Tech ช่วย — designer / FE / BE / PO
- ถ้าผู้ใช้ขอ ช่วยแก้ต่อได้เลย เริ่มจาก Blocker
